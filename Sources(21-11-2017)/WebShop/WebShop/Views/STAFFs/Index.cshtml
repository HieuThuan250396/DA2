@using WebShop.Models;
@model IndexViewModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/UI-AD/_MainPage.cshtml";
}



@*@Html.ActionLink("Create New", "Create")
    @using (Html.BeginForm())
    {
        <div class="form-group label-floating is-empty">
            <label class="control-label">Staff Name</label>
            <div class="col-md-10">
                @Html.Editor("txtFilter", "", new { htmlAttributes = new { @class = "form-control" } })
                <input type="submit" value="" id="search" />
            </div>
        </div>
        <div class="col-md-10">
            @Html.ActionLink("Hiện tất cả", "ClearAll", "STAFFs")
        </div>
    }*@
<div class="table-agile-info">
    <div class="panel panel-default">
        <div class="panel-heading">
            <p style="color:#999">Nhân Viên <a class="agile-icon" href="~/STAFFs/Create" style="float:left"><i class="fa fa-plus-square"></i></a></p>

        </div>

        <div>
            <table class="table" ui-jq="footable" ui-options='{
        "paging": {
          "enabled": true
        },
        "filtering": {
          "enabled": true
        },
        "sorting": {
          "enabled": true
        }}'>
                <tr>
                    <th data-breakpoints="xs">
                        Name
                    </th>
                    <th data-breakpoints="xs">
                        Address
                    </th>
                    <th data-breakpoints="xs">
                        Phone
                    </th>
                    <th data-breakpoints="xs">
                        Email
                    </th>

                    <th></th>
                </tr>

                @foreach (var item in Model.dsNV)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.NAME)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ADDRESS)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.PHONE)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.EMAIL)
                        </td>
                        <td>
                            <a href="/STAFFs/Edit/@item.STAFFID" class="btn btn-warning "> <i class="fa fa-pencil"></i> Sửa</a>
                            <a href="/STAFFs/Details/@item.STAFFID" class="btn btn-send"><i class="fa fa-list-alt"></i> Chi Tiết</a>
                            <a href="/STAFFs/Delete/@item.STAFFID" class="btn btn-danger"><i class="fa fa-trash-o"></i> Xóa</a>
                        </td>
                    </tr>
                }

            </table>
            @if (Model.Pager.EndPage > 1)
            {
                <ul class="pagination">
                    @if (Model.Pager.CurrentPage > 1)
                    {
                        <li>
                            <a href="~/STAFFs/Index">First</a>
                        </li>
                        <li>
                            <a href="~/STAFFs/Index?page=@(Model.Pager.CurrentPage - 1)">Previous</a>
                        </li>
                    }
                    @for (var pager = Model.Pager.StartPage; pager <= Model.Pager.TotalPages; pager++)
                    {
                        <li class="@(pager == Model.Pager.CurrentPage ?  "active" : "")">
                            <a href="~/STAFFs/Index?page=@pager">@pager</a>
                        </li>
                    }
                    @if (Model.Pager.CurrentPage < Model.Pager.TotalPages)
                    {
                        <li>
                            <a href="~/STAFFs/Index?page=@(Model.Pager.CurrentPage + 1)">Next</a>
                        </li>
                        <li>
                            <a href="~/STAFFs/Index?page=@(Model.Pager.TotalPages)">Last</a>
                        </li>
                    }
                </ul>
            }
        </div>
    </div>
</div>
